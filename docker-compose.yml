services:
  papi-app:
    build: .
    container_name: papi-backend
    ports:
      - "8089:8080"
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: always

networks:
  papi-network:
    external: true