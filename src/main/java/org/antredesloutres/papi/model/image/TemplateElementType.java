package org.antredesloutres.papi.model.image;

/** The kinds of blocks a template can place on the card. */
public enum TemplateElementType {
    NAME,
    TYPES,
    SPRITE,
    STATS,
    DESCRIPTION,
    ABILITIES,
    DEX_NUMBER
}
